name: 5 - Release

on:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  post_merge_release:
    runs-on: ubuntu-latest
    steps:
      - name: Post-merge (placeholder)
        run: |
          echo "Main atualizada. Aqui pode criar release notes, tag, etc."
